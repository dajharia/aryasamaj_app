# aryasamaj_app
A first official app of atya samaj

This repository now contains a minimal Next.js scaffold so it can be deployed to Vercel.

Run locally:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
```


